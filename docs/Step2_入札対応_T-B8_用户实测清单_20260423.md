# T-B8 用户实测清单（给项目老板照着敲）

- 任务编号：T-B8（Step2 入札対応 — customer_identifier 客户识别）
- 日期：2026-04-23
- 适用对象：项目老板（不是程序员，只需要"敲命令、看输出、判通过"）
- 全程预计：3~5 分钟

---

## 这一轮做了什么（一句话）

让系统看到一份 xlsx 投标包，能自动判断它"是不是 Customer A"。
是 → 标 `customer_a`；不是（Customer B / E / Nitori / 其他）→ 标 `unknown`，并附带一句日文/中文人话原因，让营业一看就懂。

## 你要验证什么（一句话）

按下面 6 个按钮敲 6 次命令，每次只看输出里的 1~2 个关键字。全部命中关键字 → 通过。

---

## 准备工作（敲一次就行）

打开终端（Mac 自带 Terminal 或 iTerm 都行），把下面这一坨整段复制粘贴回车：

```bash
cd /Users/zhangdongxu/Desktop/project/阪急阪神 && source .venv/bin/activate
```

回车后如果看到提示符前面变成 `(.venv)` 开头，就说明环境进去了。

---

## 按钮 1：跑全部自动化测试

复制粘贴下面这一行，回车：

```bash
python -m pytest tests/services/step2_bidding/ -v | tail -3
```

**预期最后一行看到**：`45 passed in <某个零点几>s`

看到了 → 按钮 1 通过，下一步。
看不到 / 数字不是 45 / 出现 `failed` 字样 → 停下，把整屏截图给我。

---

## 按钮 2：3 份真实 Customer A 文件，能否被认出来

```bash
cd /Users/zhangdongxu/Desktop/project/阪急阪神 && python -c "
from app.services.step2_bidding import identify
from pathlib import Path
root = Path('资料/2026.04.02/Customer A (Air)/Customer A (Air)')
for name in ['2-①.xlsx','2-②.xlsx','2-④.xlsx']:
    r = identify(root/name)
    print(f'{name:14} -> {r.matched_customer}  dims={r.matched_dimensions}  conf={r.confidence}')
"
```

**预期 3 行都是**：

```
2-①.xlsx       -> customer_a  dims=('B', 'D')  conf=high
2-②.xlsx       -> customer_a  dims=('B', 'D')  conf=high
2-④.xlsx       -> customer_a  dims=('B', 'D')  conf=high
```

3 行全是 `customer_a` + `('B', 'D')` + `high` → 按钮 2 通过。
任意一行变成 `unknown` → 停下，截图。

---

## 按钮 3：1 份 Customer B 文件，必须不被认成 A

```bash
cd /Users/zhangdongxu/Desktop/project/阪急阪神 && python -c "
from app.services.step2_bidding import identify
from pathlib import Path
r = identify(Path('资料/2026.04.02/Customer B (Ocean,LCL)/Customer B (Ocean,LCL)/2-①.xlsx'))
print('matched:', r.matched_customer)
print('reason :', r.unmatched_reason)
"
```

**预期看到**：
- 第一行：`matched: unknown`
- 第二行 `reason:` 后面那段日文+中文，里面应该有 `2025 LCL RATE` 几个字

两个关键字都对 → 按钮 3 通过。
`matched: customer_a` → 严重错误（误把别家认成 A），停下，截图。

---

## 按钮 4：1 份 Customer E 文件，必须不被认成 A

```bash
cd /Users/zhangdongxu/Desktop/project/阪急阪神 && python -c "
from app.services.step2_bidding import identify
from pathlib import Path
r = identify(Path('资料/2026.04.02/Customer E (Air & Ocean)/Customer E (Air & Ocean)/2-①.xlsx'))
print('matched:', r.matched_customer)
print('reason :', r.unmatched_reason)
"
```

**预期看到**：
- `matched: unknown`
- `reason:` 后面带 `AIR入力フォーム` 几个字

→ 按钮 4 通过。

---

## 按钮 5：1 份 Nitori 文件，必须不被认成 A

```bash
cd /Users/zhangdongxu/Desktop/project/阪急阪神 && python -c "
from app.services.step2_bidding import identify
from pathlib import Path
r = identify(Path('资料/2026.04.02/ニトリ様海上入札/ニトリ様海上入札/阪急阪神【to GLOBAL】2026年1月～3月_見積り書.xlsx'))
print('matched:', r.matched_customer)
print('reason :', r.unmatched_reason)
"
```

**预期看到**：
- `matched: unknown`
- `reason:` 后面带 `Quotation (Global) Jan-Mar` 这串字（这是 Nitori 文件里的 sheet 名）

→ 按钮 5 通过。

---

## 按钮 6：损坏文件不会让程序崩溃

```bash
cd /Users/zhangdongxu/Desktop/project/阪急阪神 && python -c "
import os, tempfile
from pathlib import Path
from app.services.step2_bidding import identify
p = Path(tempfile.mkdtemp()) / 'bad.xlsx'
p.write_bytes(os.urandom(1024))
r = identify(p)
print('matched :', r.matched_customer)
print('warning :', r.warnings)
"
```

**预期看到**：
- `matched : unknown`
- `warning :` 后面是一个元组，里面第一个字符串以 `WBOPEN_FAIL:` 开头

没有任何 Python 报错 / 异常堆栈 → 按钮 6 通过。

---

## 故障对照表（出问题查这里）

| 你看到的现象 | 含义 | 怎么办 |
|---|---|---|
| 按钮 1 的尾行是 `44 passed` 或 `46 passed` | 测试条数对不上 T-B8 设计 | 截图，回复"按钮 1 测试条数不对" |
| 按钮 1 出现 `failed` 字样 | 有用例没过 | 截图整屏，回复"按钮 1 有 failed" |
| 按钮 2 任意一行不是 `customer_a` | A 客户没被认出来（漏识别） | 截图，回复"按钮 2 漏识别" |
| 按钮 3/4/5 任意一个 `matched:` 是 `customer_a` | 误把别家认成 A（误识别，更严重） | 截图，回复"按钮 X 误识别" |
| 按钮 3 的 reason 没有 `2025 LCL RATE` / 按钮 4 没有 `AIR入力フォーム` / 按钮 5 没有 `Quotation` | 给营业看的人话原因不对 | 截图，回复"按钮 X reason 不对" |
| 按钮 6 出现 `Traceback` 或 `Error` | 损坏文件让程序崩溃了（不能上线） | 截图，回复"按钮 6 崩了" |
| 任何一步看到 `ModuleNotFoundError` 或 `ImportError` | 环境/路径不对 | 截图，回复"按钮 X 环境问题" |

---

## 收尾

6 个按钮全部按预期 → 直接回复一句：

> **通过 T-B8**

测试大师这边自动化测试结果是 45/45 通过 + 6 份真实文件实测全对 + 4 条边界全对，监工已经看过证据。你这一轮主要是亲手验一下"机器认 A 不认错"。

---

如果你只想跑最关键的一个（赶时间），那就只跑**按钮 1**。45 全过基本就是稳了。
